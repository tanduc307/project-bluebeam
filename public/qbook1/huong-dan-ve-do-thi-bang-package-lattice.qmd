# **Hướng dẫn vẽ đồ thị bằng package `lattice`** 

```{r, include=FALSE}
options(width = 1200)
knitr::opts_chunk$set(fig.width = 12, fig.height = 6, warning = FALSE, message = FALSE)
```

```{r}
library(readxl)
doanh_thu_ban_hang <- read_excel("doanh_thu_ban_hang.xlsx")
doanh_thu_ban_hang <- as.data.frame(doanh_thu_ban_hang)

doanh_thu_ban_hang
```

**Số liệu nhân viên theo giới tính và chức vụ ở các chi nhánh**

```{r}
library(lattice)

table()
```

